# 🔄 System Flow Diagrams

## 📋 Table of Contents
1. [Booking Flow](#booking-flow)
2. [Payment Verification Flow](#payment-verification-flow)
3. [Package Management Flow](#package-management-flow)
4. [User Notification Flow](#user-notification-flow)
5. [Database Relationships](#database-relationships)

---

## 1. Booking Flow

```
┌─────────────┐
│    USER     │
└──────┬──────┘
       │
       │ 1. Browse Destinations
       ▼
┌─────────────────┐
│  explorasi.php  │
└──────┬──────────┘
       │
       │ 2. Select Package
       ▼
┌─────────────────┐
│   booking.php   │
└──────┬──────────┘
       │
       │ 3. Fill Booking Form
       │    - Select package
       │    - Choose travel date
       │    - Enter quantity
       │    - Add notes
       ▼
┌─────────────────┐
│   Create Booking│
│   (bookings)    │
│   (booking_     │
│    details)     │
└──────┬──────────┘
       │
       │ 4. Redirect to Booking Detail
       ▼
┌─────────────────────┐
│ user/booking_       │
│ detail.php          │
│                     │
│ Status: PENDING     │
└─────────────────────┘
```

---

## 2. Payment Verification Flow

### User Side:

```
┌─────────────────────┐
│ user/booking_       │
│ detail.php          │
│                     │
│ Status: PENDING     │
└──────┬──────────────┘
       │
       │ 1. Upload Payment Proof
       │    - Select payment method
       │    - Enter proof URL
       ▼
┌─────────────────────┐
│   Create Payment    │
│   (payments)        │
│   status: pending   │
└──────┬──────────────┘
       │
       │ 2. Update Booking Status
       ▼
┌─────────────────────┐
│ Booking Status:     │
│ WAITING_PAYMENT     │
└─────────────────────┘
```

### Admin Side:

```
┌─────────────────────┐
│ admin/bookings.php  │
│                     │
│ 🔔 Notification: 1  │
└──────┬──────────────┘
       │
       │ 1. Click "View Detail"
       ▼
┌─────────────────────┐
│ admin/booking_      │
│ detail.php          │
│                     │
│ - View payment proof│
│ - View all details  │
└──────┬──────────────┘
       │
       ├─────────────────┬─────────────────┐
       │                 │                 │
       │ 2a. APPROVE     │ 2b. REJECT      │
       ▼                 ▼                 │
┌──────────────┐  ┌──────────────┐       │
│ Update:      │  │ Update:      │       │
│ - payment    │  │ - payment    │       │
│   status:    │  │   status:    │       │
│   approved   │  │   rejected   │       │
│ - booking    │  │ - booking    │       │
│   status:    │  │   status:    │       │
│   paid       │  │   pending    │       │
│ - Log to     │  │ - Save reason│       │
│   history    │  │ - Log to     │       │
└──────┬───────┘  │   history    │       │
       │          └──────┬───────┘       │
       │                 │               │
       │                 │               │
       └────────┬────────┘               │
                │                        │
                │ 3. Notify User         │
                ▼                        │
┌─────────────────────────────┐         │
│ user/booking_detail.php     │         │
│                             │         │
│ ✅ Payment Approved!        │◄────────┘
│    OR                       │
│ ❌ Payment Rejected         │
│    Reason: [reason text]    │
│    [Upload Again Button]    │
└─────────────────────────────┘
```

---

## 3. Package Management Flow

```
┌─────────────────────┐
│ admin/packages.php  │
│                     │
│ - List all packages │
│ - Filter by dest    │
└──────┬──────────────┘
       │
       ├──────────────┬──────────────┬──────────────┐
       │              │              │              │
       │ CREATE       │ UPDATE       │ DELETE       │
       ▼              ▼              ▼              │
┌──────────────┐ ┌──────────────┐ ┌──────────────┐│
│ Click        │ │ Click        │ │ Click        ││
│ "Add Package"│ │ "Edit"       │ │ "Delete"     ││
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘│
       │                │                │         │
       ▼                ▼                ▼         │
┌──────────────────────────────┐ ┌──────────────┐│
│ admin/package_form.php       │ │ Confirmation ││
│                              │ │ Modal        ││
│ - Select destination         │ └──────┬───────┘│
│ - Enter name                 │        │         │
│ - Enter description          │        │ Confirm │
│ - Enter price                │        ▼         │
│ - Enter duration (optional)  │ ┌──────────────┐│
│ - Enter stock (optional)     │ │ DELETE FROM  ││
│ - Toggle active/inactive     │ │ packages     ││
└──────┬───────────────────────┘ │ WHERE id = ? ││
       │                          └──────────────┘│
       │ Submit                                    │
       ▼                                           │
┌──────────────────────────────┐                  │
│ Validation                   │                  │
│ - Check required fields      │                  │
│ - Check price >= 0           │                  │
│ - Check destination selected │                  │
└──────┬───────────────────────┘                  │
       │                                           │
       │ Valid                                     │
       ▼                                           │
┌──────────────────────────────┐                  │
│ INSERT/UPDATE packages       │                  │
└──────┬───────────────────────┘                  │
       │                                           │
       │ Success                                   │
       ▼                                           │
┌──────────────────────────────┐                  │
│ Redirect to packages.php     │◄─────────────────┘
│ with success message         │
└──────────────────────────────┘
```

---

## 4. User Notification Flow

```
┌─────────────────────┐
│ Admin Action        │
│ (Approve/Reject)    │
└──────┬──────────────┘
       │
       │ 1. Update Database
       ▼
┌─────────────────────┐
│ - Update payment    │
│   status            │
│ - Update booking    │
│   status            │
│ - Save reason       │
│   (if reject)       │
│ - Log to history    │
└──────┬──────────────┘
       │
       │ 2. Set Session Message
       ▼
┌─────────────────────┐
│ $_SESSION[          │
│   'success_msg'     │
│ ]                   │
└──────┬──────────────┘
       │
       │ 3. User Visits Booking Detail
       ▼
┌─────────────────────────────────┐
│ user/booking_detail.php         │
│                                 │
│ Check payment_status:           │
│                                 │
│ IF approved:                    │
│   ✅ Show green banner          │
│   ✅ Show success message       │
│   ✅ Show "Print Ticket" button │
│                                 │
│ IF rejected:                    │
│   ❌ Show red banner            │
│   ❌ Show rejection reason      │
│   ❌ Show "Upload Again" form   │
│                                 │
│ IF pending:                     │
│   ⏳ Show waiting message       │
│   ⏳ Show upload form           │
└─────────────────────────────────┘
```

---

## 5. Database Relationships

```
┌─────────────┐
│    users    │
│─────────────│
│ id (PK)     │
│ username    │
│ email       │
│ password    │
│ role        │
└──────┬──────┘
       │
       │ 1:N
       │
       ├──────────────────────────────┐
       │                              │
       ▼                              ▼
┌─────────────┐              ┌─────────────────┐
│  bookings   │              │ payment_history │
│─────────────│              │─────────────────│
│ id (PK)     │              │ id (PK)         │
│ user_id (FK)│              │ changed_by (FK) │
│ booking_code│              │ payment_id (FK) │
│ total_amount│              │ booking_id (FK) │
│ status      │              │ old_status      │
└──────┬──────┘              │ new_status      │
       │                     │ reason          │
       │ 1:N                 │ created_at      │
       │                     └─────────────────┘
       ▼
┌─────────────────┐
│ booking_details │
│─────────────────│
│ id (PK)         │
│ booking_id (FK) │
│ package_id (FK) │
│ quantity        │
│ price_per_unit  │
│ travel_date     │
│ note            │
└──────┬──────────┘
       │
       │ N:1
       │
       ▼
┌─────────────┐
│  packages   │
│─────────────│
│ id (PK)     │
│ dest_id (FK)│
│ name        │
│ description │
│ price       │
│ stock       │
│ is_active   │
└──────┬──────┘
       │
       │ N:1
       │
       ▼
┌─────────────────┐
│  destinations   │
│─────────────────│
│ id (PK)         │
│ regency_id (FK) │
│ category_id (FK)│
│ name            │
│ slug            │
│ description     │
│ address         │
│ ticket_price    │
└─────────────────┘

┌─────────────┐
│  bookings   │
└──────┬──────┘
       │
       │ 1:1
       │
       ▼
┌─────────────────┐
│    payments     │
│─────────────────│
│ id (PK)         │
│ booking_id (FK) │
│ amount          │
│ method          │
│ payment_proof   │
│ payment_status  │
│ rejection_reason│ ← NEW
│ verified_by (FK)│
│ paid_at         │
└─────────────────┘
```

---

## 6. Status State Machine

### Booking Status:

```
┌─────────┐
│ pending │
└────┬────┘
     │
     │ User uploads payment
     ▼
┌──────────────────┐
│ waiting_payment  │
└────┬─────────────┘
     │
     ├──────────────┬──────────────┐
     │              │              │
     │ Admin        │ Admin        │
     │ approves     │ rejects      │
     ▼              ▼              │
┌────────┐    ┌─────────┐         │
│  paid  │    │ pending │◄────────┘
└────┬───┘    └─────────┘
     │
     │ Admin confirms
     ▼
┌───────────┐
│ confirmed │
└────┬──────┘
     │
     │ After travel date
     ▼
┌───────────┐
│ completed │
└───────────┘

     Any status can go to:
     ┌───────────┐
     │ cancelled │
     └───────────┘
```

### Payment Status:

```
┌─────────┐
│ pending │
└────┬────┘
     │
     ├──────────────┬──────────────┐
     │              │              │
     │ Admin        │ Admin        │
     │ approves     │ rejects      │
     ▼              ▼              │
┌──────────┐  ┌──────────┐        │
│ approved │  │ rejected │        │
└──────────┘  └────┬─────┘        │
                   │              │
                   │ User uploads │
                   │ again        │
                   └──────────────┘
```

---

## 7. Notification Badge Logic

```
┌─────────────────────────────────┐
│ admin/bookings.php              │
│                                 │
│ Query:                          │
│ SELECT COUNT(*)                 │
│ FROM bookings                   │
│ WHERE status = 'waiting_payment'│
└──────┬──────────────────────────┘
       │
       │ Result: $pendingCount
       ▼
┌─────────────────────────────────┐
│ IF $pendingCount > 0:           │
│   Show badge with count         │
│   Badge color: red              │
│   Badge position: top-right     │
│                                 │
│ ELSE:                           │
│   Hide badge                    │
└─────────────────────────────────┘
```

---

## 8. Filter & Search Logic

```
┌─────────────────────────────────┐
│ User Input:                     │
│ - Status filter (dropdown)      │
│ - Search query (text input)     │
└──────┬──────────────────────────┘
       │
       │ Submit form (GET)
       ▼
┌─────────────────────────────────┐
│ Build Query:                    │
│                                 │
│ WHERE 1=1                       │
│                                 │
│ IF status != '':                │
│   AND status = ?                │
│                                 │
│ IF search != '':                │
│   AND (booking_code LIKE ?      │
│        OR username LIKE ?)      │
└──────┬──────────────────────────┘
       │
       │ Execute query
       ▼
┌─────────────────────────────────┐
│ Display filtered results        │
│                                 │
│ Show "Clear" button if filtered │
└─────────────────────────────────┘
```

---

## 9. Transaction Flow (Payment Approval)

```
┌─────────────────────────────────┐
│ BEGIN TRANSACTION               │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 1. Get current payment status   │
│    SELECT payment_status        │
│    FROM payments                │
│    WHERE id = ?                 │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 2. Update payment status        │
│    UPDATE payments              │
│    SET payment_status='approved'│
│    WHERE id = ?                 │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 3. Update booking status        │
│    UPDATE bookings              │
│    SET status = 'paid'          │
│    WHERE id = ?                 │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│ 4. Log to payment_history       │
│    INSERT INTO payment_history  │
│    (payment_id, old_status,     │
│     new_status, changed_by)     │
└──────┬──────────────────────────┘
       │
       ├──────────────┬─────────────┐
       │              │             │
       │ Success      │ Error       │
       ▼              ▼             │
┌──────────────┐ ┌──────────────┐  │
│   COMMIT     │ │   ROLLBACK   │  │
└──────────────┘ └──────────────┘  │
                                    │
       All changes saved            │
       OR                           │
       All changes reverted◄────────┘
```

---

**Last Updated:** 26 Januari 2026  
**Version:** 1.0  
**Status:** Complete ✅
